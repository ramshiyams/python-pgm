class student:
    def __init__(self,l1,l2):
        self.l1=l1
        self.l2=l2
    def add(self,stdname):
        l1.insert(2,stdname)
        return l1
    def search(self,name):
        if name in l1:
            print("the student is present")

        else:

            print("the student is absent")
    def merge(self,l2,l1,l3):
            l2=l1+l3
            st=sorted(l2)
            return st
l1=["akhi","ramshi","rifa"]
l3=["sneha","shifa","nain"]
print(student(l1,l3))
s1=student(l1,l3)

name=input("enter the name")
print(s1.search(name))
print(s1.add(name))
print(s1.sort(l2,l1,l3))
